# Kisan Shathi

A simple crop rate prediction website with sign-up, sign-in, and a basic authentication flow.

## Run locally

1. Install dependencies
   npm install
2. Start the server
   npm start
3. Open your browser
   http://localhost:3000

   

## Notes

<!-- The app uses a PostgreSQL database through the connection string in .env. -->
 <!-- The server creates the users table automatically on first run. -->
